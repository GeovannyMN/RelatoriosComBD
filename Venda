package br.com.gnovais;

import java.sql.Time;

public record Venda(int id, float valor, String formaDePagamento, Time hora) {
    public Venda(float valor, String formaDePagamento, Time hora){
        this(-1, valor, formaDePagamento, hora);
    }
}
